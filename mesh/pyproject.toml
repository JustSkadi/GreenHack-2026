[project]
name = "greenhack-mesh"
version = "0.1.0"
description = "RL mesh routing agent — GreenHack"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "networkx",
    "hydra-core",
    "omegaconf",
    "wandb",
    "onnx",
    "onnxscript>=0.7.0",
    "matplotlib>=3.10.9",
]

[tool.uv]
package = false
